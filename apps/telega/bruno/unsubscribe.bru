meta {
  name: Unsubscribe from Channel
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/channels/unsubscribe
  body: json
  auth: none
}

body:json {
  {
    "channelId": "cryptosignalschannel"
  }
}

docs {
  # Отписаться от канала
  
  Удаляет подписку из Redis и отключается от канала в Telegram userbot.
  
  ## Request Body
  - `channelId`: имя канала без @ (например: "cryptosignalschannel")
  
  ## Response
  - `message`: статус операции
  - `channelId`: ID канала
}

